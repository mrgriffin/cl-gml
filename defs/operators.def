#ifndef OPERATOR
#error "must define OPERATOR before including operators"
#endif

OPERATOR(ADD, 0, (
	(((INT, int a), (INT, int b)), (
		push_INT(stack, a + b);
	))
))

#if 0
OPERATOR(SUB, 1, (
	(((INT, int a), (INT, int b)), (
		push_INT(stack, a - b);
	))
))
#endif

#undef OPERATOR
