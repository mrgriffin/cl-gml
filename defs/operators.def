#ifndef OPERATOR
#error "must define OPERATOR before including operators"
#endif

OPERATOR(ADD, 0, ({
	int b = pop_INT(stack);
	int a = pop_INT(stack);
	push_INT(stack, a + b);
}))

OPERATOR(SUB, 1, ({
	int b = pop_INT(stack);
	int a = pop_INT(stack);
	push_INT(stack, a - b);
}))

#undef OPERATOR
