#ifndef TYPE
#error "must define TYPE before including types"
#endif

TYPE(INT, int)
TYPE(FLOAT, float)
TYPE(OP, enum Operator)
TYPE(MARKER, enum Marker)
TYPE(ARRAY, struct Array)
TYPE(BLOCK, struct Array)

#undef TYPE
